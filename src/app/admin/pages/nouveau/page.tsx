import { PageForm } from "../PageForm";

export const dynamic = "force-dynamic";

export const metadata = {
  title: "Nouvelle page CMS — Admin ISHYA",
};

export default function NewCmsPagePage() {
  return <PageForm page={null} />;
}
